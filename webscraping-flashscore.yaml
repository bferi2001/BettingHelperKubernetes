apiVersion: apps/v1
kind: Deployment
metadata:
  name: webscraping-flashscore
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webscraping-flashscore
  template:
    metadata:
      labels:
        app: webscraping-flashscore
    spec:
      containers:
      - name: webscraping-flashscore
        image: bferi/webscraping-flashscore:1.0.0
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "1"
            memory: "1Gi"
        volumeMounts:
        - mountPath: /dev/shm
          name: dshm
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
---
apiVersion: v1
kind: Service
metadata:
  name: webscraping-flashscore-service
spec:
  selector:
    app: webscraping-flashscore
  ports:
    - port: 8003
      targetPort: 8003
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webscraping-flashscore
  labels:
    app.kubernetes.io/name: webscraping-flashscore
    storedemo.tier: backend
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.middlewares: default-webscraping-flashscore-stripprefix@kubernetescrd
spec:
  rules:
    - http:
        paths:
          - path: /api/webscraping-flashscore
            pathType: Prefix
            backend:
              service:
                name: webscraping-flashscore-service
                port:
                  number: 8003
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: webscraping-flashscore-stripprefix
spec:
  stripPrefix:
    prefixes:
      - /api/webscraping-flashscore